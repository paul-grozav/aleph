# Directory where we build the distribution
distribution_content

# editing the script
.aleph.sh.swp

# Version iso s
v*\.*\.*\.iso

# persistent drive used for emulating the core os
persistent.hdd

# Some binary file generated by XFCE4
fs/fs/root/.config/dconf/user
